{
  "private": true,
  "scripts": {
    "backend": "cd backend && python run.py",
    "frontend": "npm --prefix frontend run dev",
    "dev": "npx concurrently -n back,front -c magenta,cyan \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
